# 공간정보관리 앱 기획 정리 (mapMemo)

> 이 문서는 프로젝트 폴더 루트에 두고, Claude Code 등 AI 코딩 도구에게
> "PLANNING.md 읽고 이 내용대로 구현해줘" 라고 요청할 때 사용합니다.

## 1. 서비스 개요

지도 기반으로 공간 정보를 제공하는 앱.
- 화장실, 배드민턴장, 스터디룸, 화상면접실 등 공간 정보를 하나의 통합 구조로 관리
- 개인이 등록·관리하며, 모든 개인 데이터는 기본적으로 비공개 — 공식 데이터로의 전환은 관리자 심사를 거쳐야만 가능
- 향후 데이터 거래·병합을 고려해 JSON(JSONB) 구조를 적극 활용

> **참고 — entrance(공동현관) 카테고리 폐기 이력**: 초기 기획에는 공동현관 비밀번호를 다루는
> entrance 카테고리가 있었고, `categories.submittable=false`로 관리자 심사로도 공개 승격이
> 안 되게 하드 락하는 방식으로 법적 리스크를 차단하려 했다. 그러나 카테고리 이름은
> 사용자가 자유롭게 바꿀 수 있어(3.1), 이름 변경 한 번이면 이 락이 무의미해지는
> 신뢰할 수 없는 보호장치였다. 이에 따라 entrance 자체와 `submittable`/`access_code`를
> 서비스에서 완전히 제거했다 (`migrations/0002_remove_entrance.sql` 참고).

## 2. 플랫폼 및 개발 방식

- **플랫폼**: 모바일 + 웹 동시 대응 → Next.js 기반 PWA로 시작, 추후 필요 시 Capacitor로 앱스토어 출시
- **개발자**: 1인 개발 (바이브 코딩 활용)
- **목표**: 빠른 MVP로 아이디어 검증
- **백엔드**: Supabase (클라우드, 프로젝트명 `mapMemo`, 리전 Seoul/Tokyo 권장)
- **지도**: 카카오맵 API
- **공식 데이터 소스**: 공공데이터포털(전국 화장실 정보 등)

## 3. 데이터 구조

### 3.1 categories (동적 카테고리)
- 카테고리는 고정 enum이 아니라 **사용자가 자유롭게 생성 가능**
- 예: "부천 화장실", "서울 화장실" → 나중에 "화장실"로 병합 가능
- `canonical_id`로 병합 이력 관리 (원본 데이터 삭제 없이 이관)
- 병합은 `spaces.category_id`를 UPDATE하는 방식이라 별도 등록 과금 발생 안 함

### 3.2 spaces (통합 공간 테이블)
- 화장실, 스터디룸, 화상면접실, 배드민턴장 등 **전부 이 테이블 하나로 통합**
- 마커는 정확한 좌표(POI 참조 없음) — 사용자가 지도에 직접 찍은 좌표가 곧 데이터
  → 같은 장소, 다른 좌표 문제는 MVP 단계에서 **프론트엔드 클러스터링**으로 대응 (서버 데이터는 원본 그대로 보존)
- `managed_by` 컬럼으로 official(사이트 관리) / personal(개인 등록) 구분
- `details` JSONB로 카테고리별 세부 정보를 유연하게 저장
- `recurring_groups` — 배드민턴 클럽처럼 특정 장소에 종속된 정기 모임 정보 (장소가 상위 기준)
- **개인(personal) 데이터는 공유 기능 없음 — `visibility` 컬럼 폐지.** 개인이 등록한 데이터는 영구히 본인(`owner_id`)만 조회 가능
- **공개(official)로의 전환은 오직 관리자 심사를 통해서만 발생** (아래 3.2.1 submissions 참고). 사용자가 직접 공개 여부를 토글하는 경로는 존재하지 않음

### 3.2.1 submissions (공식 등록 심사 큐)
- 사용자가 본인 데이터를 "공식 등록 신청"하면 `submissions`에 `pending` 상태로 접수
- 관리자가 승인하면, 원본 개인 레코드를 공개 전환하는 게 아니라 **`managed_by='official'`인 별도 신규 레코드를 생성** (원본은 계속 비공개로 남음, owner_id 등 민감 필드 제외하고 공개 가능한 필드만 복사)
- 반려 시 `rejected` 상태로 기록, 원본 데이터는 그대로 개인 소유 유지
- 신고 기능은 official 승인 이후에도 문제 발견 시 대비한 보조 방어선으로 유지

### 3.3 monthly_registration_usage (과금)
- **월별 등록 건수** 기준 과금 (백업/동기화 자체는 과금 대상 아님, 자주 발생하지 않는 행위라 매출 예측성이 낮기 때문)
- 무료: 월 20건 / Basic·Pro 등 단계별 한도
- **복원(soft-delete 해제)은 예외 처리**: `created_at`을 원본 그대로 유지 → 자동으로 과거 달 카운트에 귀속되어 이번 달 한도와 무관해짐 (본인 데이터 인질 구조 방지)
- Export(내보내기)는 언제나 무료
- **타인이 export한 파일을 import하는 경우**: id/owner_id가 본인 계정 소유가 아니므로 자동으로 "신규 등록"으로 분류되어 동일한 월별 한도 과금 로직이 그대로 적용됨 (별도 기능 불필요, 기존 로직으로 충분)
- ⚠️ **과금 문구/약관 작성 원칙**: 과금 사유는 반드시 "월 무료 등록 한도 초과"로만 중립적으로 표현할 것. "타인의 정보를 가져올 때 요금이 부과된다"는 식의 표현·설계 의도는 금지 — 이는 정보 유통을 알선하고 대가를 취하는 구조로 해석되어 법적 리스크(4장 참고)가 재발할 수 있음. 실제 로직 결과는 동일해도(친구 데이터를 가져오면 과금될 가능성이 높음), 과금 기준은 항상 "출처와 무관한 순수 사용량(등록 건수)"으로 설계·설명해야 함

## 4. 법적으로 유의할 지점

- **개인이 작성한 모든 데이터는 심사 없이 공유될 수 없음** (spaces 테이블 전체에 동일 적용). 공개는 오직 관리자 심사(submissions)를 거쳐 별도 official 레코드로 생성될 때만 발생 — 사용자가 직접 공개 전환할 수 있는 경로 자체가 없으므로, "다른 카테고리에 숨겨서 공개"하는 우회도 원천 차단됨
- 데이터 형식(JSON 등 전달 방식)은 법적 책임 소재를 바꾸지 않음 — 실질(서비스가 유통을 설계했는가)이 기준
- 사용자가 export한 파일을 개인적으로 재공유하는 것은 서비스가 매개하지 않은 개인 행위이나, 약관에 "제3자 유통 기능을 제공하지 않는다"는 조항과 export 시 안내 문구를 남겨두는 것을 권장
- "직접 제공하지 않으면서 수익만 취하는" 우회 구조는 지양 (오히려 영리 목적 알선으로 판단될 리스크). 과금 설계·문구는 3.3의 원칙을 따를 것
- 실사용 전 개인정보보호법·정보통신망법 관련 변호사 자문 권장 (특히 submissions 심사 관련 기능)

## 5. 회원 관리

- Supabase Auth 표준 (이메일/비밀번호 + 카카오 소셜로그인 권장)
- "기기별 자동 회원가입"은 PWA 환경에서 기술적으로 불안정 + 법적 리스크 있어 비권장
- 대안: **동시 활성 세션 수 제한**으로 계정 공유 억제 (OTT 서비스 방식)

## 6. 수익 모델

1. 화장실/대여공간 등 비민감 카테고리의 공식 데이터 API를 지자체·기업에 라이선스 제공
2. 월별 등록 건수 기반 과금 (무료 한도 초과 시)
3. 대여공간 카테고리의 실제 대여업체 제휴 수수료

## 7. 개발 순서 (MVP 로드맵)

1. Supabase 프로젝트 생성 + schema.sql 실행 (기본 카테고리 4종 시드 포함)
2. 공공데이터포털 화장실 API 연동 → official 데이터 채우기
3. Next.js + 카카오맵으로 지도 마커 표시 (읽기 전용, official만 노출)
4. Supabase Auth 로그인/회원가입
5. 개인 등록 기능 (spaces, category_id 선택 포함, 본인만 조회되는 상태로 저장)
6. "공식 등록 신청" 버튼 + submissions 심사 큐 (관리자 승인 시에만 official로 신규 생성)
7. 월별 등록 한도 체크 + 안내 UI (문구는 "출처 무관, 무료 한도 초과" 기준으로 작성)
8. Export/Import 기능
9. PWA 설정 → 추후 Capacitor로 앱스토어 전환 검토

## 8. 참고 - 스키마 파일

전체 테이블/RLS/함수 SQL은 `schema.sql` 참고 (Supabase SQL Editor에서 실행)
