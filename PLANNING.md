# 공간정보관리 앱 기획 정리 (mapMemo)

> 이 문서는 프로젝트 폴더 루트에 두고, Claude Code 등 AI 코딩 도구에게
> "PLANNING.md 읽고 이 내용대로 구현해줘" 라고 요청할 때 사용합니다.

## 1. 서비스 개요

지도 기반으로 공간 정보를 제공하는 앱.
- 화장실, 배드민턴장, 스터디룸, 화상면접실 등 공간 정보를 하나의 통합 구조로 관리
- 개인이 등록·관리하며, 등록 시 사용자가 선택하면 다른 사용자에게 공유(검색·팔로우 대상)될 수 있음 — 공식(official) 데이터로의 승격은 여전히 관리자 심사를 거쳐야만 가능(3.2.1)
- 향후 데이터 거래·병합을 고려해 JSON(JSONB) 구조를 적극 활용

> **참고 — entrance(공동현관) 카테고리 폐기 이력**: 초기 기획에는 공동현관 비밀번호를 다루는
> entrance 카테고리가 있었고, `categories.submittable=false`로 관리자 심사로도 공개 승격이
> 안 되게 하드 락하는 방식으로 법적 리스크를 차단하려 했다. 그러나 카테고리 이름은
> 사용자가 자유롭게 바꿀 수 있어(3.1), 이름 변경 한 번이면 이 락이 무의미해지는
> 신뢰할 수 없는 보호장치였다. 이에 따라 entrance 자체와 `submittable`/`access_code`를
> 서비스에서 완전히 제거했다 (`migrations/0002_remove_entrance.sql` 참고). 이 교훈은
> 이후 커뮤니티 공유 설계(3.2.2, 4장)에도 그대로 이어진다 — 안전장치는 카테고리 이름이 아니라
> 콘텐츠 자체에 기반해야 한다.

## 2. 플랫폼 및 개발 방식

- **플랫폼**: 모바일 + 웹 동시 대응 → Next.js 기반 PWA로 시작, 추후 필요 시 Capacitor로 앱스토어 출시
- **개발자**: 1인 개발 (바이브 코딩 활용)
- **목표**: 빠른 MVP로 아이디어 검증
- **백엔드**: Supabase (클라우드, 프로젝트명 `mapMemo`, 리전 Seoul/Tokyo 권장)
- **지도**: 카카오맵 API
- **공식 데이터 소스**: 공공데이터포털(전국 화장실 정보 등)

## 3. 데이터 구조

### 3.1 categories (동적 카테고리)
- 카테고리는 고정 enum이 아니라 **사용자가 자유롭게 생성 가능**
- 예: "부천 화장실", "서울 화장실" → 나중에 "화장실"로 병합 가능
- `canonical_id`로 병합 이력 관리 (원본 데이터 삭제 없이 이관)
- 병합은 `spaces.category_id`를 UPDATE하는 방식이라 별도 등록 과금 발생 안 함

### 3.2 spaces (통합 공간 테이블)
- 화장실, 스터디룸, 화상면접실, 배드민턴장 등 **전부 이 테이블 하나로 통합**
- 마커는 정확한 좌표(POI 참조 없음) — 사용자가 지도에 직접 찍은 좌표가 곧 데이터
  → 같은 장소, 다른 좌표 문제는 MVP 단계에서 **프론트엔드 클러스터링**으로 대응 (서버 데이터는 원본 그대로 보존)
- `managed_by` 컬럼으로 official(사이트 관리) / personal(개인 등록) 구분
- `details` JSONB로 카테고리별 세부 정보를 유연하게 저장
- `recurring_groups` — 배드민턴 클럽처럼 특정 장소에 종속된 정기 모임 정보 (장소가 상위 기준)
- **`shared` 컬럼(boolean, 기본 true)으로 개인(personal) 데이터의 커뮤니티 공유 여부를 사용자가
  직접 선택**한다 — 등록 폼의 "공유하기" 체크박스(기본 ON, 끌 수 있음)로 결정되며, 이 명시적
  선택 자체가 4장의 법적 방어선 중 하나가 된다
- `personal` + `shared=true`인 레코드는 다른 로그인 사용자에게 검색·팔로우 대상이 됨(3.2.2의
  콘텐츠 필터를 통과한 것만). `shared=false`이거나 비로그인 사용자에게는 여전히 본인
  (`owner_id`)만 조회 가능
- RLS `spaces_read`는 `managed_by='official' OR owner_id=auth.uid() OR (managed_by='personal'
  AND shared=true)`로 확장
- **공식(official)으로의 승격은 이 커뮤니티 공유와 완전히 별개**로, 여전히 오직 관리자 심사를
  통해서만 발생한다(3.2.1 참고). 커뮤니티 공유는 사용자 간 공유일 뿐, 공식 데이터로 자동
  승격되지 않는다

> **참고 — `visibility` 컬럼 재도입 이력**: v2에서 `visibility` 컬럼을 완전히 제거하고 개인
> 데이터를 영구 비공개로 만들었던 이유는, 당시엔 공개 전환 경로 자체가 없어야 안전하다고
> 판단했기 때문이다. 이후 "활발한 정보 공유"가 서비스 핵심 가치로 재확인되면서, 이번에는
> `shared` 컬럼으로 공개 전환 경로를 다시 연다 — 다만 entrance 사고의 교훈(카테고리 이름
> 기반 안전장치는 무력화됨)을 반영해, 이번 안전장치는 카테고리가 아니라 **콘텐츠 필터 +
> 신고/사후조치 + 명시적 사용자 액션**(3.2.2, 4장)에 기반한다.

### 3.2.1 submissions (공식 등록 심사 큐)
- 사용자가 본인 데이터를 "공식 등록 신청"하면 `submissions`에 `pending` 상태로 접수
- 관리자가 승인하면, 원본 개인 레코드를 공개 전환하는 게 아니라 **`managed_by='official'`인 별도 신규 레코드를 생성** (원본은 계속 비공개로 남음, owner_id 등 민감 필드 제외하고 공개 가능한 필드만 복사)
- 반려 시 `rejected` 상태로 기록, 원본 데이터는 그대로 개인 소유 유지
- 신고 기능은 official 승인 이후에도 문제 발견 시 대비한 보조 방어선으로 유지

### 3.2.2 커뮤니티 공유 & 콘텐츠 안전장치
- **콘텐츠 필터**: `shared=true`로 등록/수정 시 `details.name`·`details.description` 텍스트를
  검사해 비밀번호·전화번호·특정인 식별 패턴이 감지되면 서버 액션에서 등록을 거부하고 재작성을
  요구한다. 카테고리 이름이 아니라 **내용 자체**를 스캔하므로 entrance 사고처럼 카테고리 이름을
  바꿔 우회할 수 없다 (`src/lib/usage.ts`의 "DB 트리거는 카운트만, 차단은 애플리케이션에서"
  패턴과 동일한 앱 레벨 체크 철학)
- **신고 & 사후조치**: 로그인한 사용자는 공유된(`shared=true`) space를 신고할 수 있다
  (`reports` 테이블, `pending`/`resolved` 상태). 관리자 1인이 `/admin/reports`에서 신고를
  검토해 필요 시 해당 space를 `shared=false`로 전환한다(soft take-down — 소유권은 유지, 원본
  데이터는 삭제되지 않음)
- 이 흐름은 사전 심사가 아니라 **사후 대응(notice-and-takedown)** 구조다. official 승격
  심사(3.2.1)는 "서비스가 검증을 보증하고 제3자에 라이선싱하는" 별개의 더 높은 책임 구간이라
  계속 관리자의 사전 심사를 거친다

### 3.3 profiles & 닉네임
- `profiles` 테이블: `id`(=`auth.users.id`), `nickname`(unique, not null) — 이메일을 다른
  사용자에게 노출하지 않기 위한 별도 공개 프로필
- 회원가입 시 닉네임을 함께 입력받아 `profiles` row를 생성한다(5장 회원 관리 참고)
- 그룹은 화면에 **`"{닉네임}_{카테고리명}"`** 형태로 표시되어 검색·구분에 사용된다. 단, 이는
  UI 표시 방식일 뿐 — **카테고리 테이블 자체는 그대로 전역 공유·병합 가능한 taxonomy**를
  유지한다(3.1 변경 없음). "그룹"은 스키마상 새 개념이 아니라 `(owner_id, category_id)`
  조합을 이렇게 표시하는 것이다

### 3.4 monthly_registration_usage (과금) — ⚠️ 보류·재검토 대상
- 기존에는 월별 등록 건수 기준으로 무료 한도(월 20건)를 두고 초과 시 과금하는 구조였다 (아래
  세부 내용은 그 구조에 대한 과거 기록)
- **"활발한 정보 공유"라는 새 방향과 상충할 수 있어 재검토 대상**: 등록 건수 자체를 제한하면
  가장 활발히 공유하는 사용자를 오히려 억제하게 된다. 6장에서 정리했듯 과금 방식은 공유
  모델·법적 안전장치가 실제로 구현된 뒤 새로 설계하며, 그때 이 월별 한도를 그대로 가져갈지,
  다른 기준(저장 용량, 프리미엄 기능 등)으로 대체할지, 완전히 없앨지를 결정한다
- **코드 상태**: `src/lib/usage.ts`의 `checkMonthlyLimit`는 차단(`allowed`)을 항상 `true`로
  반환하도록 이미 비활성화했다 — 등록을 더 이상 막지 않는다. `monthly_registration_usage`
  테이블과 집계 트리거(`increment_registration_count`)는 그대로 두어 사용량 데이터는 계속
  쌓이므로, 과금 모델을 새로 설계할 때 참고 자료로 쓸 수 있다. 테이블·트리거·UI를 완전히
  걷어낼지는 아직 미결정(7장 17번)

<details>
<summary>과거 상세 로직 (재검토 전까지 참고용)</summary>

- **월별 등록 건수** 기준 과금 (백업/동기화 자체는 과금 대상 아님, 자주 발생하지 않는 행위라 매출 예측성이 낮기 때문)
- 무료: 월 20건 / Basic·Pro 등 단계별 한도
- **복원(soft-delete 해제)은 예외 처리**: `created_at`을 원본 그대로 유지 → 자동으로 과거 달 카운트에 귀속되어 이번 달 한도와 무관해짐 (본인 데이터 인질 구조 방지)
- Export(내보내기)는 언제나 무료
- **타인이 export한 파일을 import하는 경우**: id/owner_id가 본인 계정 소유가 아니므로 자동으로 "신규 등록"으로 분류되어 동일한 월별 한도 과금 로직이 그대로 적용됨 (별도 기능 불필요, 기존 로직으로 충분)

</details>

- ⚠️ **과금 문구/약관 작성 원칙 (재검토 후에도 유효)**: 과금 사유는 반드시 "월 무료 등록 한도
  초과"처럼 순수 사용량 기준으로만 표현할 것. "타인의 정보를 가져올 때 요금이 부과된다"는 식의
  표현·설계 의도는 금지 — 이는 정보 유통을 알선하고 대가를 취하는 구조로 해석되어 법적 리스크
  (4장 참고)가 재발할 수 있음. 향후 과금 모델을 무엇으로 새로 설계하든 이 원칙은 계속 적용됨

### 3.5 follows
- `follows` 테이블: 로그인 사용자가 **회원(user)** 또는 **그룹**(`owner_id`+`category_id`
  조합) 단위로 팔로우할 수 있다
- 팔로우는 **승인 없이 자유롭게** 생성·해제 가능 (승인제/비공개 계정식은 "활발한 공유" 목표와
  충돌해 채택하지 않음 — 자세한 검토는 4장 참고)
- 팔로우 효과: 지도의 그룹 태그 필터(`KakaoMap.tsx`)에 팔로우한 회원·그룹의 `shared=true`
  데이터가 자동으로 추가 노출된다

## 4. 법적으로 유의할 지점

### 4.1 개인 데이터 공유의 법적 방어선 (v3 재정립)
초기 설계는 "개인 데이터는 심사 없이 공유될 수 없음"을 절대 원칙으로 삼아 개인 데이터를
영구 비공개로 뒀다. 이후 "활발한 정보 공유"가 서비스 핵심 가치로 재확인되면서, 3.2/3.2.2의
`shared` + 콘텐츠 필터 + 신고 구조로 방향을 전환했다. 이 전환이 법적으로 방어 가능한 이유:

- **entrance 사고의 교훈**: 카테고리 이름 기반 안전장치(`submittable=false`)는 사용자가
  이름을 바꾸는 순간 무력화됐다. 따라서 새 안전장치는 카테고리 이름이 아니라 **콘텐츠(이름·
  설명 텍스트) 자체**를 스캔하는 필터에 기반한다(3.2.2) — 카테고리를 무엇으로 부르든 우회 불가
- **"팔로워 전용 공개"는 방어선이 아니다**: 팔로우가 승인 없이 자유로운 이상(3.5), 팔로워
  전용 노출은 사실상 전체공개와 동일한 도달 범위다. 따라서 법적 방어선은 노출 범위를 좁히는
  데서 나오지 않고, 아래 네 가지 장치에서 나온다
- **네 가지 안전장치**: ①등록 시 사용자의 명시적 "공유하기" 액션(기본 ON이라도 끌 수 있는
  실제 선택지가 존재), ②콘텐츠 필터(3.2.2), ③신고 + 관리자 1인의 사후조치
  (notice-and-takedown, 3.2.2), ④닉네임 표시(3.3)를 통한 책임 추적성. 이 중 ①이 특히 중요 —
  "서비스가 유통을 일방적으로 설계하지 않고, 사용자가 스스로 선택했다"는 사실이 3.4(과금)
  원칙과 동일한 방어 논리를 제공한다
- **official 승격은 별개로 계속 관리자 심사가 필요**: 커뮤니티 공유(사용자 간)와 official
  승격(서비스가 검증을 보증하고 제3자에 라이선싱)은 책임 수준이 다르다. official은 지금처럼
  submissions 심사를 거쳐야만 발생하며, 사용자가 직접 official로 전환하는 경로는 없다(3.2.1)

### 4.2 기타 유의점
- 데이터 형식(JSON 등 전달 방식)은 법적 책임 소재를 바꾸지 않음 — 실질(서비스가 유통을
  설계했는가)이 기준
- 사용자가 export한 파일을 개인적으로 재공유하는 것은 서비스가 매개하지 않은 개인 행위이나,
  약관에 "제3자 유통 기능을 제공하지 않는다"는 조항과 export 시 안내 문구를 남겨두는 것을 권장
- "직접 제공하지 않으면서 수익만 취하는" 우회 구조는 지양 (오히려 영리 목적 알선으로 판단될
  리스크). 과금 설계·문구는 3.4의 원칙을 따를 것
- 실사용 전 개인정보보호법·정보통신망법 관련 변호사 자문 권장 (특히 커뮤니티 공유·신고 처리,
  submissions 심사 관련 기능)

## 5. 회원 관리

- Supabase Auth 표준 (이메일/비밀번호 + 카카오 소셜로그인 권장)
- 회원가입 시 닉네임을 함께 입력받아 `profiles` row를 생성한다(3.3 참고) — 이메일은 비공개로
  유지되고, 닉네임만 다른 사용자에게 노출된다
- "기기별 자동 회원가입"은 PWA 환경에서 기술적으로 불안정 + 법적 리스크 있어 비권장
- 대안: **동시 활성 세션 수 제한**으로 계정 공유 억제 (OTT 서비스 방식)

## 6. 수익 모델

> ⚠️ 아래는 이전 모델(개인 데이터 영구 비공개 원칙) 기준으로 짜여진 잠정안이다. **정보 공유
> 활성화(3.2.2/3.5)와 4장의 법적 안전장치가 실제로 구현·개선된 뒤, 과금 방식은 그 결과를
> 바탕으로 처음부터 새롭게 검토한다** — 지금 단계에서 세부 항목을 미리 확정하지 않는다.
> 그 전까지는 아래 항목을 잠정 기준으로만 유지한다.

1. 화장실/대여공간 등 비민감 카테고리의 공식 데이터 API를 지자체·기업에 라이선스 제공
2. 월별 등록 건수 기반 과금 (무료 한도 초과 시)
3. 대여공간 카테고리의 실제 대여업체 제휴 수수료

## 7. 개발 순서 (MVP 로드맵)

> 상태 표시: ✅ 완료 · ⚠️ 부분 완료(확인/설정 필요) · ⬜ 미착수

1. ✅ Supabase 프로젝트 생성 + schema.sql 실행 (기본 카테고리 4종 시드 포함)
2. ⚠️ 공공데이터포털 화장실 API 연동 → official 데이터 채우기
   — 관리자 화면(`/admin/submissions`)에 가져오기 버튼과 적재 로직(`src/lib/publicData/toilets.ts`, `/api/admin/import-toilets`)은 구현됨.
   실제 데이터셋 URL·서비스키(`DATA_GO_KR_SERVICE_KEY`/`DATA_GO_KR_TOILET_API_URL`)가 아직 없고, 필드 매핑도 흔한 후보명으로 추정만 해둔 상태라 실제 응답으로 검증 필요
3. ✅ Next.js + 카카오맵으로 지도 마커 표시 (읽기 전용, official만 노출) — `/`, `src/components/KakaoMap.tsx`
4. ⚠️ Supabase Auth 로그인/회원가입 — 이메일/비밀번호(`/login`)만 구현. 카카오 소셜로그인은 Supabase Provider 설정이 추가로 필요해 미구현(5장 참고)
5. ✅ 개인 등록 기능 — 메인 지도(`/`)의 등록 버튼 → 지도 클릭/드래그로 위치 지정 → 모달에서 카테고리 선택(또는 신규 생성)·이름·설명 입력 (`src/components/KakaoMap.tsx`, `src/components/RegisterSpaceModal.tsx`). 주소 필드는 제거됨
6. ✅ "공식 등록 신청" 버튼 + submissions 심사 큐 — `/spaces`에서 신청, `/admin/submissions`에서 관리자 승인/반려
7. ⚠️ 월별 등록 한도 체크 + 안내 UI — `src/lib/usage.ts`의 `checkMonthlyLimit`는 카운트 조회
   로직은 그대로 있으나, 3.4/17에 따라 **차단(`allowed`)은 항상 true로 비활성화**된 상태
   (사용량 집계 자체는 DB 트리거로 계속 쌓임)
8. ✅ Export/Import 기능 — `/spaces` 페이지, `/api/spaces/export`·`/api/spaces/import`
9. ✅ PWA 설정 (`public/manifest.json` + `public/sw.js`, 최소 오프라인 캐싱) → Capacitor 전환은 아직 미착수
10. ✅ profiles/닉네임 — 회원가입 폼(`/login`)에 닉네임 필드 추가, `auth.users` insert 트리거
    `handle_new_user()`로 `profiles` row 자동 생성 (3.3). 가입 전 중복 확인은
    `src/app/login/actions.ts`의 `checkNicknameAvailable`(service_role)로 처리
11. ✅ `spaces.shared` 컬럼 + RLS 확장 — `RegisterSpaceModal`의 "공유하기" 체크박스(기본 ON),
    RLS `spaces_read`에 `(managed_by='personal' and shared=true and auth.uid() is not null)`
    조건 추가로 로그인 사용자에게만 공유 데이터 노출 (3.2)
12. ✅ 콘텐츠 필터 — `src/lib/contentFilter.ts`, `createSpace`에서 `shared=true` 등록 시 검사해
    거부 (3.2.2)
13. ✅ 신고 + `/admin/reports` — `src/app/spaces/actions.ts`의 `reportSpace`(지도 마커 정보창의
    "신고" 버튼에서 호출), `src/app/admin/reports/`(목록·기각·비공개 전환 UI, 비공개 전환 시
    실제 `shared=false` 반영) (3.2.2)
14. ⚠️ follows — `migrations/0012_follows.sql`(`follower_id`+`owner_id`+`category_id` 조합,
    RLS `follows_read/insert/delete_own`)까지 구현됨(3.5). 다만 팔로우를 새로 추가/해제하는
    화면은 아직 없음(8.4 참고) — 지금은 이미 존재하는 관계를 사이드바에서 조회·토글하는
    것까지만 지원
15. ✅ 사이드바 — `src/components/FollowSidebar.tsx`가 공식 데이터/내 마커/팔로우한 타인 마커
    3단 그룹 트리 + 체크박스로 재설계됨(8.4 참고)
16. ✅ 지도 그룹 필터에 팔로우한 회원·그룹의 공유 데이터 반영 — `src/components/KakaoMap.tsx`가
    그룹 단위 지연 로딩(체크 시에만 조회, 캐시 재사용)으로 반영(8.4 참고)
17. ⚠️ 월별 등록 한도(3.4) 적용 여부 재검토 — 차단 로직은 `src/lib/usage.ts`에서 비활성화
    완료(항상 `allowed: true`). 유지/기준 변경/완전 제거(테이블·트리거·UI 정리) 중 최종 결정은
    아직 미착수

## 8. 개선 사항 백로그

### 8.1 ✅ 지도 롱프레스/우클릭으로 등록 진입 (버튼 방식 대체)
"+ 등록" 버튼을 제거. 지도를 500ms 이상 누르고 있으면 **손을 떼기 전에 즉시**(`pointerdown`에서
예약한 `setTimeout`이 만료되는 시점) 누른 지점에 미니 컨텍스트 메뉴("여기에 등록")가 뜬다.
데스크톱은 **우클릭**(`contextmenu`)으로도 동일하게 바로 뜬다 — 둘 다 공통 함수
`triggerMenuAt(clientX, clientY)`로 귀결(`src/components/KakaoMap.tsx`). 임계 시간 전 10px
이상 이동하면 타이머를 취소해 카카오맵 자체 팬 제스처와 충돌하지 않는다. "여기에 등록" 선택 시
바로 `RegisterSpaceModal`이 열린다.

누르고 있는 도중(아직 카카오맵 자체의 `click`/`rightclick` 이벤트가 없는 시점)에 위치가
필요해, 화면 픽셀 좌표를 `map.getBounds()`(확실히 존재하는 API)로 위경도 범위에 선형 보간해
직접 계산하는 `pixelToLatLng()`를 추가했다 — 불확실한 `Projection` 류 API에는 의존하지 않음
(`src/types/kakao-maps.d.ts`에 `Map.getBounds()`/`LatLngBounds` 타입 추가).

⚠️ 구현 중 발견한 버그 2건: ①(1차 구현) 롱프레스 판정 좌표를 너무 일찍 `null`로 지워 메뉴가
화면 밖에 렌더링됨 → `setTimeout` 기반으로 재작성하며 근본 해결. ②우클릭 자동화 테스트 중
Playwright의 locator 기반 클릭이 지도 요소의 bounding box를 헤더와 겹치는 것으로 오판하는
자동화 도구 이슈를 발견(기능 자체엔 문제 없음, `contextmenu` 이벤트를 직접 dispatch해 핸들러
동작을 확인). Playwright 실사용 검증(누른 채로 메뉴 확인 → 뗌, 우클릭 즉시 메뉴, 모달 오픈)
완료.

### 8.2 ✅ 그룹 필터 UX 강화 — 탭(소유 범위/카테고리) + 선택 칩
기존 "전체 선택/초기화/공식만 보기/내 등록만 보기" 4버튼과 평탄한 태그 목록을 없애고,
그룹을 **소유 범위**(공식/내 등록/타인 닉네임)와 **카테고리** 두 개의 독립된 facet으로 분해해
탭으로 나눠 고르는 구조로 재설계(`src/components/KakaoMap.tsx`). 선택된 항목은 탭과 무관하게
하단에 칩으로 모아 보여주고 칩의 ×로 개별 제거, "초기화"로 전체 해제. 검색은 활성 탭의 옵션만
필터링.

⚠️ 구현 중 발견한 버그: 처음엔 "선택 안 함(null)=전체 선택"으로 취급해 옵션이 하나뿐인 facet을
클릭하면 오히려 해제되는 반직관적 동작이 있었음 → "선택 없음=전체 표시, 하나라도 고르면 그것만
표시"로 모델을 단순화해 해결(빈 Set과 null을 동일하게 취급). Playwright로 탭 전환·칩 생성/제거·
전체 표시 복귀까지 검증 완료.

**사이드바 뼈대**: "팔로우" 토글 버튼 + `src/components/FollowSidebar.tsx` 추가 — 지도 위에
겹쳐 오른쪽에서 슬라이드 인/아웃하는 패널(지도 크기는 그대로 유지). 이미 로드된 그룹 데이터로
"닉네임_그룹이름" 검색까지는 동작하지만, 실제 팔로우 영속화(`follows` 테이블, 팔로우 시
지도 필터 반영)는 **다음 단계**로 남겨둠 — 로드맵 14~16번 착수 시 이어서 구현.

### 8.3 ✅ 지인에게 명시적으로 개인 기록 공유(초대)하기
`space_invites` 테이블(토큰·7일 만료·소유자 CRUD만 RLS 허용) 추가. 소유자가 `/spaces`에서
"초대 링크" 버튼(`SpaceRowActions.tsx` → `createInvite`)을 누르면 `shared` 값과 무관하게 해당
space를 볼 수 있는 링크가 생성되어 클립보드에 복사된다. 링크(`/i/[token]`)는 service_role로
토큰을 조회해 소유자 닉네임과 함께 공간 정보를 보여주고, 로그인 사용자는 "내 공간에 저장"으로
본인 계정에 `가져오기(import)`할 수 있다(`src/app/i/[token]/`) — 3.4의 월별 한도 로직이 그대로
적용되며, 병합(merge)은 여전히 owner 본인 데이터끼리만 가능하다는 8.3(구 8.3) 범위 제한과 무관.
`createInvite`는 space_id가 실제로 요청자 소유인지 별도 검증한다(타인 소유 space_id로 초대를
만들 수 없도록). 로그인 유도를 위해 `/login`에 `?next=` 리다이렉트 파라미터 지원 추가.
Playwright로 초대 생성 → 열람 → 가져오기 → DB 반영까지 실사용 검증 완료.

### 8.4 ✅ 그룹 트리 사이드바(지연 로딩) + 마커 정보창 3줄 포맷 + 모바일 뷰포트 수정
지도 상단에 몰려 있던 검색창/탭/칩/초기화/"팔로우" 버튼을 전부 걷어내고, 상단에는 등록 안내
배지 하나만 남겼다(`src/components/KakaoMap.tsx`). 사이드바(`FollowSidebar.tsx`)는 화면
우측 가장자리에 항상 붙어있는 토글 탭(☰/›)으로 여닫으며, 내부는 **공식 데이터 / 내 마커 /
팔로우한 타인 마커** 3단 그룹 트리 + 체크박스로 재설계했다(8.2의 탭+칩 필터를 대체). "새로운
그룹 팔로우하기"(팔로우 추가/해제 화면)는 이번 범위가 아니라 여전히 준비 중 문구만 있음.

**그룹 단위 지연 로딩**: 기존엔 로그인 사용자가 볼 수 있는 모든 space를 한 번에 불러와
클라이언트에서만 필터링했으나, 이제 그룹 목록(가벼운 메타데이터, `category_id`+카테고리명만)과
실제 마커 데이터(위치/상세정보)를 분리했다. 체크 해제는 서버 통신 없이 클라이언트에서만 숨기고,
체크는 캐시에 없을 때만 해당 그룹을 조회하며, 한 번 조회한 그룹은 재체크해도 캐시를 재사용해
서버 통신이 없다(좌표/거리 기반 부분 재조회 같은 복잡한 분기는 범위 밖으로 확정). `follows`
테이블(`migrations/0012_follows.sql`)을 새로 추가해 "팔로우한 타인 마커" 그룹 목록을 실제
팔로우 관계로 걸러 보여준다.

**마커 정보창**은 1줄 이름/2줄 평가(★ 1~5점, 0.5단위, 선택입력)/3줄 설명(10자 초과 시 "…")
3줄 포맷으로 바뀌었다. 평가는 별도 컬럼 없이 기존 `details` JSONB 패턴을 따라
`details.rating`으로 저장(`src/app/spaces/actions.ts`, `RegisterSpaceModal.tsx`).

**모바일 뷰포트 버그**: 실기기에서 마커 등록 모달이 화면 오른쪽으로 밀려 잘리고 옆으로
드래그하면 페이지 전체가 밀리는 문제를 발견 — `src/app/layout.tsx`의 `viewport` export에
`width: "device-width"`/`initialScale: 1`이 빠져 모바일 브라우저가 데스크톱 기준 가상
뷰포트(~980px)로 렌더링했던 것이 원인이었다. 추가로 `globals.css`의 `body`에
`overflow-x: hidden`도 방어적으로 추가.

⚠️ Playwright 실사용 검증 중 카카오맵 마커는 실제 DOM 좌표 기반으로 직접 클릭 좌표를 찾아야
정보창이 열림(로케이터 기반 클릭은 지도 캔버스에 막혀 반응하지 않음 — 8.1에서 발견한 것과
같은 종류의 자동화 도구 이슈, 기능 자체엔 문제 없음). 체크박스 캐시 재사용(네트워크 탭으로
서버 요청 없음 확인), 등록 직후 "내 마커" 그룹 즉시 반영(새 카테고리 포함), 모바일 폭에서
등록 모달 중앙 정렬까지 실사용 검증 완료.

## 9. 참고 - 스키마 파일

전체 테이블/RLS/함수 SQL은 `schema.sql` 참고 (Supabase SQL Editor에서 실행)
